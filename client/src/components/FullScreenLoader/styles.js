const styles = theme => ({
  progress: {
    margin: theme.spacing(2)
  },
  loaderContainer: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    fontSize: "2em",
    color: "#F9A825",
    background: "black",
    textAlign: "center"
  }
});

export default styles;
