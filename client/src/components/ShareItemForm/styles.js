const styles = theme => ({
  shareform: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    backGround: "purple"
  }
});

export default styles;
